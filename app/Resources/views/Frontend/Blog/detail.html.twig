{% extends ':Frontend:base.html.twig' %}

{% block title %}{{ post.title }} · Blog{% endblock %}

{% block main %}
    <section>
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    {% include ':Frontend/includes:image_detail.html.twig' with {object: post, filter: '1200xY'} %}
                    <h2>{{ post.title }}</h2>
                    <h3 class="text-muted"><i class="fa fa-calendar c-orange-md" aria-hidden="true"></i> {{ post.publishedAt | date('d/m/Y') }} | por {{ post.author.fullname }}</h3>
                    {% for tag in post.tags %}
                        {% spaceless %}
                            <a class="c-blue-md" href="{{ path('front_blog_tag_detail', {slug: tag.slug}) }}">{{ tag.title }}</a>
                            {% if loop.index != loop.last %}, {% endif %}
                        {% endspaceless %}
                    {% endfor %}
                    <p class="text-muted">{{ post.description | raw }}</p>
                </div>
                <div class="col-md-4">
                    {% include ':Frontend/includes:_side.html.twig' %}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
