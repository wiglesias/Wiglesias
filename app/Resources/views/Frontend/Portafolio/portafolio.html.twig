{% extends ':Frontend:base.html.twig' %}

{% block title %}Portafolio{% endblock %}

{% block main %}
    <div class="p-vertical text-center">
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <h2>Portafolio</h2>
                    <h3>Aquí se encuentran todos mis trabajos y colaboraciones realizados con otros proyectos.</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            {% if allPortafolios | length > 0 %}
                {% for portafolio in allPortafolios %}
                <div class="col-md-4">
                    <div class="thumbnail">
                        {% include ':Frontend/includes:image_detail.html.twig' with {object: portafolio, filter: '780xY'} %}
                        <div class="caption">
                            <h4>{{ portafolio.title }}</h4>
                            <p>{{ portafolio.description |truncate(100) | raw }}</p>
                        </div>
                    </div>
                </div>
                {% if loop.index is divisible by(3) %}
                    </div>
                    <div class="row extra-m-top">
                {% endif %}
                {% endfor %}
            {% else %}
                {% include ':Frontend/includes:under_construciton_warning.html.twig' %}
            {% endif %}
        </div>
    </div>
{% endblock %}
