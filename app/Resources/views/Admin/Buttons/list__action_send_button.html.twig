{% if admin.isGranted('EDIT', object) and admin.hasRoute('send') %}
    <a href="{{ admin.generateObjectUrl('send', object) }}" class="btn btn-sm btn-default edit_link" title="pdf"><i class="fa fa-envelope-o"></i></a>
{% endif %}
